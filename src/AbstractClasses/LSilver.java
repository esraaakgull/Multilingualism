package AbstractClasses;

import Classes.User;

public class LSilver extends League{
    public LSilver(User[] userList) {
        super(userList);
    }

    @Override
    void checkConditions() {
        super.checkConditions();
    }
}
