package AbstractClasses;

import Classes.User;

public class LRuby extends League{
    public LRuby(User[] userList) {
        super(userList);
    }

    @Override
    void checkConditions() {
        super.checkConditions();
    }
}
